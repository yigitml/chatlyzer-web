name: Remote Update (CD)

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          # FIX 1: Upgraded to Node 22 to satisfy Prisma requirements
          node-version: '22' 
          #cache: 'npm'

      - name: Create Environment File
        # FIX 2: Named exactly .env so Prisma's postinstall script can find DATABASE_URL
        run: echo "${{ secrets.PROD_ENV_FILE }}" > .env

      - name: Install & Build
        run: |
          # npm ci
          # Note: Since your package.json runs prisma generate automatically 
          # during postinstall, we actually don't need a separate step for it here!
          npm install
          npm run build

      - name: Copy built files to server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 2222
          # Added src/generated/ so the server gets your custom Prisma client!
          # Also removed strip_components: 1 as it can sometimes swallow root files like package.json
          source: ".next/,public/,package.json,package-lock.json,prisma/,src/generated/" 
          target: "/home/prod/chatlyzer-web"

      - name: Update Server and Restart
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 2222
          script: |
            cd /home/prod/chatlyzer-web
            
            npm ci --omit=dev
            
            # We removed 'npx prisma generate' because GitHub already did it!
            npx prisma migrate deploy
            
            pm2 restart 0